# Results

This repo does not ship trained models. The evaluation tables below can be generated from benchmark artifacts (JSON/CSV) produced during training.

Next: add `docs/artifacts/report-*.json` and `docs/artifacts/cv-all.csv`, then run `python scripts/make_results_md.py`.
