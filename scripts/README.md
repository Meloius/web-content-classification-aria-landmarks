## Models are not committed

This repository does not include trained model artifacts (`.sav`/`.joblib`).

To reproduce training, use the reference implementation:
- https://github.com/watinha/aria-landmarks-identification

Copy the trained artifacts into `models/` if you want to run prediction locally.
